#include <fstream>
#include <string>
#include <iostream>
#include <vector>
using namespace std;


int main() {
    ifstream infile("input.txt");


    for (string line; getline(infile,line);) {
    }
    return 0;
}